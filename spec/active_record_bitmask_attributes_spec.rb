require "spec_helper"

describe ActiveRecordBitmaskAttributes do
  it "has a version number" do
    expect(ActiveRecordBitmaskAttributes::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
